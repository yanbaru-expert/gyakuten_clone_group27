<div class="container">
  <div class="row">
    <% @movies.each.with_index(1) do |movie, i| %>
      <div class="col-12 col-md-6 col-lg-4">
        <div class="card border-dark mb-3">
          <div class="card-header p-0">
            <div class="embed-responsive embed-responsive-16by9">
              <iframe width="560" height="315" src="<%= movie.url %>" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>     
            </div>
          </div>
          <div class="card-body text-dark movie-body">
            <div id="watched-movie">
              <object>
                <% if movie.watched_by?(current_user) %>
                  <%= link_to "読破済みを解除する", movie_watched_movies_path(movie), class: "btn btn-primar btn-block", method: :delete %>
                <% else %>
                  <%= link_to "読破済みにする",movie_watched_movies_path(movie), class: "btn btn-secondary btn-block", method: :post %>
                <% end %>
              </object>
            </div>
            <p class="card-text"><%= movie.title %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <%= paginate @movies %>
</div>
